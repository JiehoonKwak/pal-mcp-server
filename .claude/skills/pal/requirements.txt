# PAL Skills Dependencies
# =======================
# Install with: pip install -r requirements.txt

# Core dependencies
pyyaml>=6.0

# AI Provider SDKs (install at least one)
# ----------------------------------------

# Google Gemini
google-genai>=1.0.0

# OpenAI (also used for X.AI, OpenRouter, custom endpoints)
openai>=1.0.0

# Optional: HTTP client for custom endpoints
httpx>=0.24.0
